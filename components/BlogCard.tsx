import { BlogPost } from '@/data/blogs';

// Blog card component - to be implemented
// Specs: Two-column layout, date 20%, content 80%
// Spacing between entries: 3rem
// "Read article →" link in mint green

interface BlogCardProps {
  post: BlogPost;
}

export default function BlogCard(_props: BlogCardProps) {
  // Component to be implemented
  return null;
}
